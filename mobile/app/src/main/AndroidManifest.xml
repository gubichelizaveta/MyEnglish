<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.EnglishApp"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LogInActivity"></activity>
        <activity android:name=".activity.RegistrationActivity"></activity>
        <activity android:name=".HomePage"></activity>
        <activity android:name=".activity.ModulesActivity"/>
        <activity android:name=".activity.CardsActivity"/>
        <activity android:name=".activity.TestActivity"/>
        <activity android:name=".activity.FavoritesModulesActivity"/>
        <activity android:name=".activity.TestCategoryActivity"/>
        <activity android:name=".activity.ProgressActivity"/>

    </application>

</manifest>